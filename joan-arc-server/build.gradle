description = "Joan API classes for the ArcGIS Server REST API"
version = rootProject.version

repositories {
    mavenCentral()
}

dependencies {
    compile project(':joan-arc-core')
    testCompile group: 'junit', name: 'junit', version: '4.11'
}
